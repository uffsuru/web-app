# This is an example file.
# Copy this to a new file named .env and fill in your actual database credentials.
# The .env file is ignored by git, so your credentials will not be committed.

# PostgreSQL database credentials
DB_USER="postgres"
DB_PASSWORD="your_postgres_password"
DB_HOST="localhost"
DB_NAME="auction"